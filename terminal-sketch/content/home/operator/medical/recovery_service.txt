MEDICAL // RECOVERY SERVICE

The chamber controller accepts the chamber number and the date of the last manual override as proof of access. Both are recorded in the clinical note; admission and scan dates are not valid.

Credential format: MR-<chamber>-<override MMDD>
Keep the month and day together, without a slash. A valid credential opens Neural Link verification. Successful waveform alignment releases medical access and opens Communications.

COMMAND REFERENCE: auth help

PATIENT SAFETY: cortex_echo.app verifies conscious responses if a sleep order is active. A passing result provides a Cortex attestation. Authorize it, then combine the three ROOT shares to cancel the order.

A valid credential begins Neural Link verification; it does not release access alone. Match the reference waveform and lock the parameters. Interrupted verification can be resumed with the neural_link.app application.
