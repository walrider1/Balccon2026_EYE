MEDICAL // RECOVERY SERVICE

The chamber controller accepts the occupied chamber number and the date of its last signed manual override as proof of access. Ward placement and interventions are held by separate records desks. Admission, automated procedures and unsigned requests carry no override authority.

Credential format: MR-<chamber>-<override MMDD>
Keep the month and day together, without a slash. Medical clearance requires a verified patient waveform.

COMMAND REFERENCE: auth help

PATIENT SAFETY: cortex_echo.app verifies conscious responses if a sleep order is active. A passing result provides a Cortex attestation. Authorize it, then combine the three ROOT shares to cancel the order.

