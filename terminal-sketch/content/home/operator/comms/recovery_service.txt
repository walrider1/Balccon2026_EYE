COMMUNICATIONS // RESTORE THE UPLINK

The controller needs the blocked distress packet number and the time its request was blocked. Packet and lock registers use a shared request ID. The evidence review explains the recorder conventions.
Credential: F-<packet>-<HHMM>. Remove the colon from the time.
Use the comms authorization domain. Command reference: auth help.

Receiver acceptance threshold: frequency and polarization locks at 92% or higher for eight continuous seconds. Both are required for a stable link.

The receiver separates carrier strength from frequency lock. Find the incoming carrier, refine its frequency, then align polarization. Interrupted decoding starts again; both locks must hold together. Select a dial with 1 or 2, use Left/Right, and hold Shift for fine steps.
